// Package auth implements the `papermap auth` CLI subcommands: login,
// logout, and whoami. The login form is a one-shot huh.Form so it stays
// independent of the main Bubble Tea program.
package auth
